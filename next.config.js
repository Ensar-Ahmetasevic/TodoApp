const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["aws-ensar-uploads.s3.eu-north-1.amazonaws.com"],
  },
};

module.exports = nextConfig;

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   reactStrictMode: true,
// };

// module.exports = nextConfig;
